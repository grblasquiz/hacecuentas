#!/bin/sh
# Pre-commit hook de hacecuentas. Activar con:
#   git config core.hooksPath .githooks
#
# Checks:
#  1) check-slug-renames: si renombraste el `slug` de una calc, pedir 301.
exec node scripts/check-slug-renames.mjs
