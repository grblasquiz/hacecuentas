# Carpeta de la colección calcs-pt-pt (Portugal, pt-PT).
# Vacía a propósito: otro agente la llena con los JSON de las calcs.
# El glob loader de Astro (pattern *.json) matchea 0 archivos sin romper el build.
