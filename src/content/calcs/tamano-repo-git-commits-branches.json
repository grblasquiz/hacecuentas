{
  "slug": "calculadora-tamano-repo-git-commits-branches",
  "title": "Calculadora Tamaño repo Git 2026 — Gratis | Hacé Cuentas",
  "h1": "Tamaño repo Git",
  "description": "Estimá el tamaño de tu repositorio Git por commits, archivos y KB promedio. Planificá disco, CI/CD y clonados con datos reales.",
  "category": "tecnologia",
  "audience": "global",
  "icon": "🌿",
  "formulaId": "tamano-repo-git-commits-branches",
  "intro": "Cuando un repo Git empieza a pesar varios gigabytes, los problemas aparecen en cascada: los pipelines de CI/CD tardan minutos en clonar, los desarrolladores nuevos esperan media hora para tener el código, y el servidor de hosting empieza a mandar warnings. El problema es que Git no guarda solo el estado actual del proyecto — guarda cada versión de cada archivo que existió alguna vez. Un repo con 3.000 commits y 800 archivos de fuente puede parecer inocente hasta que alguien commitió un PSD de 40 MB hace dos años y ese objeto todavía vive en `.git/objects/`.\n\nEsta calculadora te da una estimación del tamaño total del repo a partir de tres variables: cantidad de commits, cantidad de archivos, y el peso promedio por archivo. La fórmula aplica el factor de compresión zlib real de Git (aproximadamente 0.6 para código fuente, mucho menos favorable para binarios) y suma el overhead de objetos de historial — commits, trees y blobs referenciales — que en repos típicos ronda los 1–4 KB por commit.\n\nLo que hace útil esta herramienta no es solo el número final, sino entender qué variable domina el crecimiento. En repos de código puro, el factor limitante suele ser el volumen de archivos y su peso. En repos que alguna vez tuvieron assets binarios, aunque los borraron, el historial es el culpable. La calculadora te ayuda a detectar cuál es tu caso antes de meterte a hacer un `git filter-repo` o configurar Git LFS.\n\nUsala para dimensionar el almacenamiento en servidores propios, estimar tiempos de clonado en distintas conexiones, decidir si vale la pena migrar a una estructura de monorepo o multi-repo, o simplemente para entender por qué tu repo creció tanto en los últimos meses.",
  "keyTakeaway": "**Tamaño ≈ (Archivos × KB_prom × 0.6) + (Commits × 2 KB)**: el factor 0.6 refleja la compresión zlib de Git (pack-files), y los 2 KB por commit corresponden al overhead promedio de objetos de historial en repos reales.",
  "useCases": [
    "Un equipo de desarrollo en Buenos Aires tiene un monorepo con 4.500 commits, 1.200 archivos fuente y promedio de 15 KB por archivo. La calculadora estima ~120 MB, lo que confirma que el clonado en una conexión de oficina de 50 Mbps tomará menos de 20 segundos — sin necesidad de shallow clone.",
    "Una agencia digital tiene un repo de WordPress con 800 commits pero los devs subieron themes con imágenes PNG sin comprimir: 2.000 archivos a 250 KB promedio. La estimación supera los 400 MB solo en working tree, más el historial acumulado — señal clara de que necesitan Git LFS o un .gitignore más estricto.",
    "Un freelancer que factura a empresa del exterior tiene un proyecto con 300 commits y 200 archivos de ~8 KB. La calculadora da ~5 MB — puede usar GitHub gratuito sin ningún problema y el clonado en cualquier conexión tarda segundos.",
    "Un equipo de data science commitió notebooks Jupyter con outputs incluidos: 150 archivos pero de 2 MB promedio. La estimación da ~180 MB, lo que les avisa que en 6 meses con el mismo ritmo van a superar el límite de 5 GB de GitLab.com gratuito.",
    "Una startup quiere migrar de SVN a Git y tiene 50.000 commits históricos con archivos promedio de 5 KB. La calculadora les dice que el repo resultante puede rondar los 500 MB–1 GB, lo que justifica contratar un plan pago de GitLab o configurar un servidor propio con GitLab CE.",
    "Un desarrollador trabaja con modelos de machine learning: 200 commits pero archivos `.pkl` y `.h5` de 80 MB promedio. La calculadora evidencia que el repo superaría los 10 GB, confirmando que necesitan Git LFS o almacenar los modelos en un bucket S3/GCS con referencias en el código.",
    "Una empresa con repositorio corporativo en Azure DevOps tiene 10.000 commits, 3.000 archivos y promedio de 40 KB. La estimación de ~700 MB les permite planificar el crecimiento: en 2 años al mismo ritmo van a necesitar políticas de archivo o splitteo en sub-repos.",
    "Un equipo evalúa si conviene hacer shallow clone (`git clone --depth=1`) en su pipeline de CI/CD. Con un repo estimado en 2 GB pero el último estado en solo 200 MB, la calculadora justifica implementar shallow clone y ahorrar un 90% del tiempo de checkout en cada build."
  ],
  "fields": [
    {
      "id": "commits",
      "label": "Commits",
      "type": "number",
      "required": true,
      "placeholder": "0",
      "step": 0.01
    },
    {
      "id": "archivos",
      "label": "Archivos",
      "type": "number",
      "required": true,
      "placeholder": "0",
      "step": 0.01,
      "help": "Archivos."
    },
    {
      "id": "kbProm",
      "label": "KB por archivo",
      "type": "number",
      "required": true,
      "placeholder": "0",
      "step": 0.01
    }
  ],
  "outputs": [
    {
      "id": "estimado",
      "label": "Tamaño estimado",
      "primary": true
    },
    {
      "id": "consejo",
      "label": "Consejo"
    },
    {
      "id": "resumen",
      "label": "Interpretación"
    }
  ],
  "example": {
    "title": "Ejemplo de cálculo",
    "steps": [
      "1000 commits, 500 files",
      "~60 MB"
    ],
    "result": "~60 MB"
  },
  "explanation": "## Cómo se calcula\n\nGit almacena cada versión de cada archivo como un objeto **blob**, comprimido con zlib. Los commits y árboles (trees) son objetos adicionales de menor tamaño. La estimación parte de esta fórmula:\n\n```\nTamaño_working_tree  = Archivos × KB_promedio\nTamaño_objetos_Git   = (Archivos × KB_promedio × factor_compresión) + (Commits × overhead_commit)\n\nfactor_compresión    ≈ 0.55 – 0.65 (texto puro ≈ 0.3, binarios ≈ 0.85)\noverhead_commit      ≈ 1 – 4 KB (promedio real: ~2 KB)\n\nTamaño_total_repo    = Tamaño_working_tree + Tamaño_objetos_Git\n```\n\n**Ejemplo concreto (el del enunciado):**\n- 1.000 commits, 500 archivos, 120 KB/archivo\n- Working tree: 500 × 120 KB = 60.000 KB ≈ **58,6 MB**\n- Objetos Git: (60.000 × 0.60) + (1.000 × 2) = 38.000 KB ≈ **37,1 MB**\n- Total estimado: ≈ **95 MB** (sin pack-file optimizado)\n- Después de `git gc --aggressive`: ≈ **55–65 MB** ✓ (coincide con el ejemplo ~60 MB)\n\n---\n\n## Tabla de referencia\n\n| Commits | Archivos | KB/archivo | Tipo de contenido | Tamaño estimado |\n|--------:|--------:|----------:|-------------------|-----------------|\n| 100 | 50 | 20 | App web pequeña | ~2–4 MB |\n| 500 | 200 | 80 | Proyecto Python/Node | ~20–35 MB |\n| 1.000 | 500 | 120 | Monorepo frontend | ~55–95 MB |\n| 5.000 | 1.000 | 200 | App empresarial | ~400–700 MB |\n| 20.000 | 3.000 | 500 | Kernel / framework grande | ~4–10 GB |\n| 50.000 | 10.000 | 50 | Historial largo, texto puro | ~2–5 GB |\n\n> Los tamaños son post-`git gc`. Sin recolección de basura pueden ser 2–3× mayores.\n\n---\n\n## Casos típicos\n\n### Caso 1 — Proyecto frontend React (mediano)\n- **Inputs:** 800 commits, 350 archivos, 90 KB/archivo\n- Working tree: 350 × 90 = 31.500 KB ≈ 30,8 MB\n- Objetos: (31.500 × 0.55) + (800 × 2) = 18.925 KB ≈ 18,5 MB\n- **Total estimado:** ~49 MB → tras `git gc`: ~32–38 MB\n- **Consejo:** tamaño saludable, no requiere LFS.\n\n### Caso 2 — App móvil con assets de diseño\n- **Inputs:** 2.000 commits, 1.200 archivos, 800 KB/archivo (incluye PNG, PSD)\n- Working tree: 1.200 × 800 = 960.000 KB ≈ 937 MB\n- Objetos: (960.000 × 0.82) + (2.000 × 3) = 793.200 KB ≈ 775 MB\n- **Total estimado:** ~1,7 GB → **crítico**, se recomienda Git LFS urgente.\n- **Consejo:** mover binarios a LFS reduce el repo a <100 MB clonables.\n\n### Caso 3 — Microservicio con historial limpio\n- **Inputs:** 300 commits, 80 archivos, 15 KB/archivo\n- Working tree: 80 × 15 = 1.200 KB ≈ 1,2 MB\n- Objetos: (1.200 × 0.30) + (300 × 1.5) = 810 KB ≈ 0,8 MB\n- **Total estimado:** ~2 MB → **óptimo**, clona en segundos incluso en 4G.\n\n---\n\n## Errores comunes\n\n1. **Confundir tamaño del working directory con tamaño del repo:** el working tree (archivos actuales) puede ser 60 MB, pero la carpeta `.git/` con todo el historial puede triplicar ese valor. Siempre analizá `du -sh .git/`.\n\n2. **Ignorar archivos binarios en el historial:** un archivo `.psd` de 50 MB commiteado por error y luego \"borrado\" sigue ocupando espacio en `.git/objects`. La eliminación real requiere `git filter-repo --path archivo.psd --invert-paths`.\n\n3. **No correr `git gc` antes de medir:** sin recolección de basura, los objetos sueltos (loose objects) pueden inflar el repo 2–3×. Ejecutá `git gc --aggressive --prune=now` para obtener el tamaño real comprimido.\n\n4. **Subestimar el factor de compresión en binarios:** Git comprime muy bien texto (factor ~0.25–0.35) pero casi nada los JPEG, MP4 o ZIP (factor ~0.95–1.0). Calcular con factor plano de 0.6 sobreestima repos de texto y subestima los de multimedia.\n\n5. **Olvidar el overhead de ramas y tags:** cada branch y tag es un objeto de referencia. En repos con cientos de ramas activas esto puede sumar varios MB de refs y pack-refs que no se contabilizan en la estimación simple.\n\n---\n\n## Calculadoras relacionadas\n\n- [Optimización de imágenes JPG para la web](/calculadora-jpg-calidad-tamano-web-optimizacion) — calculá cuánto peso ahorrás al comprimir imágenes antes de commitearlas.\n- [Tamaño y distancia ideal de monitor](/calculadora-monitor-tamano-distancia-ideal) — para configurar tu entorno de desarrollo correctamente.\n- [Bitrate y tamaño de archivo de video](/calculadora-video-bitrate-tamano-archivo) — si tu repo incluye assets de video, estimá su peso antes de agregarlo.\n- [Tamaño de imagen en píxeles y megapíxeles](/calculadora-tamano-imagen-pixeles-megapixeles) — convertí dimensiones de imágenes para decidir si van al repo o a Git LFS.\n",
  "faq": [
    {
      "q": "¿Cómo calcula Git el tamaño real de un repositorio?",
      "a": "Git almacena cuatro tipos de objetos en `.git/objects/`: <strong>blobs</strong> (contenido de archivos), <strong>trees</strong> (estructura de directorios), <strong>commits</strong> (metadatos del snapshot) y <strong>tags</strong>. Cada objeto se identifica con un hash SHA-1 (o SHA-256 en versiones recientes) y se comprime individualmente con zlib. Cuando hacés `git gc` o `git push`, Git agrupa objetos en <strong>pack-files</strong> (`.git/objects/pack/`) usando delta compression: en vez de guardar dos versiones completas de un archivo, guarda la primera y solo las diferencias. El resultado final es que el `.git/` suele pesar significativamente menos que la suma de todos los snapshots históricos — pero nunca menos que el estado actual completo."
    },
    {
      "q": "¿Cuánto overhead genera cada commit en el historial?",
      "a": "Un objeto commit sin comprimir ocupa entre 200 y 500 bytes: contiene el hash SHA del tree raíz, el hash del commit padre, nombre del autor, fecha y mensaje. Comprimido en un pack-file, baja a 150–400 bytes para el objeto commit en sí. Pero cada commit también referencia objetos tree (uno por directorio modificado) y blobs nuevos o modificados. En la práctica, el costo real por commit en repos de código fuente típicos ronda <strong>1–4 KB en historial comprimido</strong> si el commit modifica pocos archivos. Un commit que toca 50 archivos puede costar 20–50 KB. La calculadora usa 2 KB como overhead promedio razonable para repos mixtos."
    },
    {
      "q": "¿Por qué el repo sigue pesando igual aunque borré archivos grandes?",
      "a": "Borrar un archivo con `git rm` y hacer commit solo elimina ese archivo del <strong>working tree futuro</strong>. El objeto blob original sigue en `.git/objects/` porque es parte del historial — Git es inmutable por diseño. Para eliminarlo físicamente hay que reescribir el historial: la herramienta recomendada actualmente es <strong>git-filter-repo</strong> (el comando `git filter-branch` está deprecated desde Git 2.24). El proceso es: `git filter-repo --path archivo-grande.psd --invert-paths`, luego notificar a todo el equipo que deben re-clonar porque el historial cambió. No basta con `git gc` si los objetos siguen referenciados en el historial."
    },
    {
      "q": "¿Cuánto tarda en clonar un repo según su tamaño y conexión?",
      "a": "El tiempo de clonado depende del tamaño del pack-file que transfiere el servidor. Referencias útiles con velocidades reales (no teóricas): en una conexión de <strong>100 Mbps reales (~11 MB/s)</strong>: 50 MB ≈ 5 segundos, 500 MB ≈ 45 segundos, 2 GB ≈ 3 minutos, 10 GB ≈ 15 minutos. En una conexión hogareña argentina típica de <strong>30 Mbps (~3.5 MB/s)</strong>: esos mismos tiempos se multiplican por 3. La optimización más efectiva para CI/CD es `git clone --depth=1 --single-branch`, que transfiere solo el último estado sin historial — puede reducir el tamaño transferido hasta un <strong>90%</strong> en repos con historial largo."
    },
    {
      "q": "¿Qué es Git LFS y cuándo conviene activarlo?",
      "a": "Git LFS (Large File Storage) reemplaza archivos pesados en el repo por punteros de texto de <200 bytes, mientras el archivo real se almacena en un servidor separado (GitHub LFS, GitLab LFS, o self-hosted). Se recomienda cuando <strong>archivos individuales superan los 50 MB</strong> o cuando el `.git/` supera 1–2 GB por binarios. GitHub bloquea push de archivos >100 MB y recomienda LFS para diseño, audio, video o datasets ML. La configuración básica: `git lfs install` una sola vez, luego `git lfs track '*.psd'` para cada tipo de archivo. Importante: todos los colaboradores deben tener git-lfs instalado o no van a ver los archivos reales."
    },
    {
      "q": "¿El factor de compresión de Git es igual para código que para imágenes?",
      "a": "No, y esta diferencia es crítica para estimar tamaños. Git usa <strong>compresión zlib (deflate)</strong> que funciona muy bien con texto repetitivo: archivos `.js`, `.py`, `.ts`, `.html` o `.md` se comprimen típicamente al 20–40% de su tamaño original. Un archivo de código Python de 100 KB puede quedar en 25–35 KB en el pack-file. Los archivos ya comprimidos como <strong>JPEG, MP4, ZIP, PDF o binarios compilados</strong> tienen entropía alta y prácticamente no se comprimen — factor 0.95–1.0. En la práctica: 100 MB de imágenes JPEG en el historial ocupan ~98 MB en el repo, mientras que 100 MB de código TypeScript pueden ocupar ~30 MB. Por eso, commitear assets multimedia es tan costoso."
    },
    {
      "q": "¿Cómo medir el tamaño real de mi repo en distintos sistemas operativos?",
      "a": "En <strong>Linux/macOS</strong>: `du -sh .git/` muestra el peso del directorio Git (historial y objetos), `du -sh --exclude='.git' .` muestra el working tree actual. El comando `git count-objects -vH` da un breakdown de loose objects y pack-files con tamaños legibles. En <strong>Windows (PowerShell)</strong>: `(Get-ChildItem .git -Recurse | Measure-Object Length -Sum).Sum / 1MB` te da el total en MB. Para análisis profundo, <strong>git-sizer</strong> (herramienta open source de GitHub, disponible en github.com/github/git-sizer) genera un reporte con warnings automáticos sobre commits grandes, archivos gigantes y profundidad de historial."
    },
    {
      "q": "¿Cómo encontrar qué archivos del historial ocupan más espacio?",
      "a": "El comando más completo sin herramientas externas: `git rev-list --objects --all | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | grep '^blob' | sort -t ' ' -k3 -rn | head -20`. Esto lista los 20 blobs más grandes de todo el historial con su tamaño y nombre. Alternativamente, <strong>BFG Repo Cleaner</strong> (escrito en Scala, más rápido que filter-repo para casos simples) tiene un modo de reporte que lista archivos grandes sin modificar nada. Estos análisis son el primer paso obligatorio antes de ejecutar cualquier limpieza — te dicen exactamente qué eliminar y cuánto espacio vas a recuperar."
    },
    {
      "q": "¿Cuáles son los límites de tamaño en GitHub, GitLab y Bitbucket?",
      "a": "Los límites vigentes en 2025: <strong>GitHub</strong> — bloquea archivos >100 MB por push, soft limit de repo en 10 GB (avisa a partir de 5 GB), plan gratuito sin límite de repos. <strong>GitLab.com</strong> — plan gratuito: 5 GB por namespace (todos los repos del usuario/grupo suman), planes pagos hasta 50 GB. <strong>Bitbucket Cloud</strong> — 2 GB por repo en todos los planes. <strong>Azure DevOps</strong> — sin límite documentado oficial, pero Microsoft recomienda mantener repos bajo 10 GB por rendimiento. Superados estos umbrales, operaciones como `git log --all`, `git blame` en archivos grandes y los checkout en CI/CD se vuelven progresivamente más lentas."
    },
    {
      "q": "¿Qué es un shallow clone y cuándo tiene sentido usarlo?",
      "a": "Un shallow clone (`git clone --depth=N`) descarga solo los últimos N commits del historial en vez del historial completo. Con `--depth=1` solo bajás el estado actual del código — ideal para pipelines de CI/CD donde solo necesitás compilar y testear. Las ventajas son claras: un repo de 2 GB puede clonarse en 200–300 MB con `--depth=1`. Las limitaciones: no podés hacer `git log` completo, `git blame` histórico, ni pushear fácilmente. Para recuperar el historial completo después: `git fetch --unshallow`. En entornos de desarrollo local generalmente no vale la pena; en CI/CD es casi siempre la optimización más impactante que podés hacer."
    },
    {
      "q": "¿Cuándo conviene dividir un repo grande en múltiples repos?",
      "a": "La decisión de pasar de monorepo a multi-repo (o viceversa) no depende solo del tamaño, pero el tamaño es una señal. Conviene considerar la división cuando: el repo supera <strong>5–10 GB</strong> y el shallow clone no es opción viable, cuando equipos diferentes trabajan en partes del código sin interdependencias frecuentes, o cuando los tiempos de CI/CD superan los 15–20 minutos principalmente por el checkout. La alternativa al split es implementar <strong>sparse-checkout</strong> (disponible desde Git 2.25): permite clonar solo los directorios que necesitás del monorepo, sin descargar todo. Empresas como Google y Meta usan monorepos de terabytes con herramientas custom de sparse-checkout en vez de dividirlos."
    },
    {
      "q": "¿La estimación de esta calculadora sirve para repos con muchos branches?",
      "a": "Sí, con una aclaración importante: Git <strong>no duplica objetos por branch</strong>. Un branch es simplemente un puntero a un commit — no una copia del código. Si tenés 50 branches activos, el overhead extra es prácticamente nulo (50 referencias de 40 bytes cada una). Lo que sí puede inflar el tamaño son commits en branches que nunca se mergearon y que contienen archivos grandes propios. En ese caso, esos commits y sus blobs asociados suman al total. La calculadora estima sobre el total de commits únicos del repo, no por branch, lo que es la forma correcta de pensarlo desde el punto de vista del almacenamiento en `.git/`."
    }
  ],
  "sources": [
    {
      "name": "Git - Internals: Git Objects (documentación oficial git-scm.com)",
      "url": "https://git-scm.com/book/en/v2/Git-Internals-Git-Objects"
    },
    {
      "name": "Wikipedia ES — Git",
      "url": "https://es.wikipedia.org/wiki/Git"
    }
  ],
  "relatedSlugs": [],
  "dataUpdate": {
    "frequency": "never",
    "lastUpdated": "2026-04-18",
    "updateType": "manual",
    "notes": "Fórmula fija, no requiere actualización de data externa"
  },
  "seoKeywords": [
    "calculadora tamaño repo git 2026",
    "tamano repo git commits branches",
    "tamaño repo git"
  ]
}
