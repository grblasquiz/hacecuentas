{
  "slug": "calculadora-imagen-docker-capas-peso-mb",
  "lastReviewed": "2026-05-23",
  "title": "Calculadora Imagen Docker (capas/tamaño) 2026 | Hacé Cuentas",
  "h1": "Imagen Docker (capas/tamaño)",
  "description": "Calculá el tamaño total de tu imagen Docker sumando imagen base y capas. Compará Alpine vs Debian vs Ubuntu y optimizá antes del push.",
  "category": "tecnologia",
  "audience": "global",
  "icon": "🐳",
  "formulaId": "imagen-docker-capas-peso-mb",
  "intro": "Cuando hacés `docker push` y tardás 4 minutos en subir una imagen que podría pesar 10 veces menos, el problema no es tu conexión: es cómo construiste la imagen. Esta calculadora te ayuda a estimar el tamaño total de una imagen Docker sumando el peso de la imagen base más las dependencias y capas adicionales que instalás durante el build, antes de que pierdas tiempo y plata en registros como Docker Hub, Amazon ECR o GitHub Container Registry.\n\nEn Docker, cada instrucción `RUN`, `COPY` o `ADD` en el Dockerfile genera una capa inmutable que se apila sobre la anterior. El tamaño final es la suma comprimida de todas esas capas. El problema es que muchos equipos arrancan con la imagen oficial de Node (~900 MB) o Python (~900 MB) sin preguntarse si realmente necesitan todo lo que trae, y terminan con pipelines de CI/CD lentos y facturas de almacenamiento más altas de lo necesario.\n\nLa elección de la imagen base es la decisión más impactante: Alpine Linux pesa ~5 MB, Debian Slim ~80 MB, Ubuntu ~77 MB, la imagen oficial de Node ~900 MB y la de Python ~920 MB. Sumale capas mal consolidadas (un `RUN apt-get install` por línea en vez de uno solo con `&&`) y podés duplicar o triplicar el tamaño sin notarlo.\n\nEsta calculadora te da un número concreto para comparar escenarios: ¿cuánto bajás si migrás a Alpine? ¿Cuánto ahorrás con un multi-stage build? ¿Vale la pena usar `scratch` o `distroless` para tu binario Go? Con el tamaño estimado podés calcular costos de storage en ECR (USD 0,10 por GB/mes), tiempos de pull en tus nodos y capacidad necesaria en registros privados como Harbor o Nexus. Mejor saberlo antes del push que después de la factura.",
  "keyTakeaway": "**Tamaño total ≈ Imagen base + Σ Capas de dependencias**. Ejemplo real: Alpine (5 MB) + Node.js compilado desde fuente (~45 MB) = ~50 MB vs. imagen `node:20` oficial = ~900 MB. Usar multi-stage builds puede reducir el tamaño hasta un **90%**.",
  "useCases": [
    "Un equipo de desarrollo tiene una imagen Node.js basada en `node:20` que pesa 920 MB. Estimando base Alpine (5 MB) + Node compilado (45 MB) + dependencias npm (80 MB), proyectan bajar a ~130 MB: un 86% menos de almacenamiento en ECR y pull times de 8 seg en vez de 55 seg.",
    "Una startup que deployea en AWS ECS tiene 50 versiones de su imagen en ECR a 800 MB cada una: 40 GB de storage a USD 0,10/GB/mes = USD 4/mes. Migrando a imágenes de 150 MB, el costo baja a USD 0,75/mes.",
    "Un pipeline de GitHub Actions tarda 4 minutos en hacer pull de la imagen base antes de correr tests. Calculando que Alpine + dependencias pesaría 120 MB en vez de 900 MB, el equipo proyecta reducir ese paso a menos de 40 segundos.",
    "Un SRE planifica capacidad para un registro privado Harbor que va a alojar 200 imágenes de microservicios. Estimando promedio de 300 MB por imagen, calcula 60 GB de storage necesario y elige el tier de almacenamiento adecuado antes de provisionar.",
    "Un dev está migrando una app Go de una imagen `golang:1.22` (~800 MB) a un multi-stage build con imagen final `scratch` (0 MB base + binario estático ~12 MB). La calculadora le confirma que el resultado final sería ~12 MB, validando que el esfuerzo de migración tiene sentido.",
    "Un equipo de ML usa una imagen Python con TensorFlow que pesa 3,2 GB. Calculando base slim (80 MB) + TensorFlow CPU (600 MB) + deps adicionales (200 MB), estiman que la versión optimizada pesaría ~880 MB: 72% menos, lo que cambia radicalmente sus costos de transferencia en GCR.",
    "Un arquitecto de plataforma evalúa si conviene usar `node:20-alpine` (~170 MB) o `node:20-slim` (~240 MB) para 80 microservicios en un clúster Kubernetes. La diferencia de 70 MB por imagen, multiplicada por 80 servicios y 10 versiones en caché por nodo, representa 56 GB de disco por nodo.",
    "Un freelancer argentino que trabaja para un cliente europeo necesita justificar el tiempo de refactoring del Dockerfile. Muestra que la imagen actual pesa 1,1 GB y la proyectada pesa 95 MB, traduciendo eso en USD 12/mes de ahorro en Docker Hub Pro y pipelines 3x más rápidos."
  ],
  "fields": [
    {
      "id": "base",
      "label": "Base",
      "type": "select",
      "required": true,
      "options": [
        {
          "value": "alpine",
          "label": "Alpine (~5 MB)"
        },
        {
          "value": "debian-slim",
          "label": "Debian slim (~70)"
        },
        {
          "value": "ubuntu",
          "label": "Ubuntu (~80)"
        },
        {
          "value": "distroless",
          "label": "Distroless (~20)"
        }
      ],
      "default": "alpine"
    },
    {
      "id": "deps",
      "label": "Deps MB",
      "type": "number",
      "required": true,
      "placeholder": "0",
      "step": 0.01
    }
  ],
  "outputs": [
    {
      "id": "total",
      "label": "Tamaño total",
      "primary": true
    },
    {
      "id": "consejo",
      "label": "Consejo"
    },
    {
      "id": "resumen",
      "label": "Interpretación"
    }
  ],
  "example": {
    "title": "Ejemplo de cálculo",
    "steps": [
      "Alpine + 50MB",
      "~55 MB"
    ],
    "result": "~55 MB"
  },
  "explanation": "## Cómo se calcula\n\nEl tamaño de una imagen Docker es la suma de todas sus capas (layers). Cada capa es inmutable y se identifica por su hash SHA-256. El tamaño que muestra `docker images` es el tamaño **descomprimido** (en disco), mientras que el tamaño de transferencia (push/pull) es el **comprimido** (aprox. 40-60% del original).\n\n```\nTamaño total (disco) = Σ Peso de cada capa\n\nTamaño transferencia ≈ Tamaño total × 0.45  (ratio de compresión típico gzip)\n\nCapas = Base layer + RUN layer 1 + RUN layer 2 + ... + COPY layer N\n```\n\nCon multi-stage build:\n```\nTamaño final = Solo capas del stage final (sin herramientas de compilación)\nAhorro = Tamaño single-stage − Tamaño multi-stage\n```\n\n## Tabla de referencia\n\n| Imagen base           | Tamaño disco | Tamaño comprimido | Sistema base        | Uso típico                     |\n|-----------------------|:------------:|:-----------------:|---------------------|-------------------------------|\n| `scratch`             | 0 MB         | 0 MB              | Vacío               | Binarios Go estáticos          |\n| `alpine:3.19`         | ~7 MB        | ~3 MB             | Alpine Linux / musl | Microservicios, CLIs           |\n| `debian:12-slim`      | ~80 MB       | ~30 MB            | Debian / glibc      | Apps Python, Ruby              |\n| `ubuntu:24.04`        | ~77 MB       | ~29 MB            | Ubuntu / glibc      | General purpose                |\n| `node:20-alpine`      | ~130 MB      | ~51 MB            | Alpine + Node.js    | Apps Node.js livianas          |\n| `node:20`             | ~1.1 GB      | ~430 MB           | Debian + Node.js    | Node.js con todas las libs     |\n| `python:3.12-slim`    | ~130 MB      | ~50 MB            | Debian Slim + Python| Apps Python                    |\n| `python:3.12`         | ~1.0 GB      | ~390 MB           | Debian + Python     | Python con compiladores        |\n| `openjdk:21-slim`     | ~250 MB      | ~95 MB            | Debian Slim + JDK   | Apps Java                      |\n| `golang:1.22-alpine`  | ~250 MB      | ~100 MB           | Alpine + Go toolchain| Solo para build stage          |\n\n## Casos típicos\n\n### Caso 1: API Node.js sin optimización vs. con Alpine\n\n**Sin optimizar:**\n- Base: `node:20` → 1.1 GB\n- `npm install` (producción) → +150 MB\n- Código fuente (COPY) → +5 MB\n- **Total: ~1.255 GB** 🔴\n\n**Con Alpine + multi-stage:**\n- Stage 1 (build): `node:20-alpine` → 130 MB + deps dev → descartado\n- Stage 2 (prod): `node:20-alpine` → 130 MB + solo `node_modules` prod (50 MB) + código (5 MB)\n- **Total: ~185 MB** ✅ → **Reducción del 85%**\n\n---\n\n### Caso 2: Binario Go con `scratch`\n\n```dockerfile\n# Stage 1: compilación\nFROM golang:1.22-alpine AS builder\nWORKDIR /app\nCOPY . .\nRUN go build -o api .\n\n# Stage 2: imagen final\nFROM scratch\nCOPY --from=builder /app/api /api\nENTRYPOINT [\"/api\"]\n```\n- Build stage: 250 MB (descartado)\n- **Imagen final: ~8 MB** (solo el binario estático) ✅\n\n---\n\n### Caso 3: App Python con dependencias ML (sin optimizar)\n\n- Base: `python:3.12` → 1.0 GB\n- `pip install pandas numpy scikit-learn` → +600 MB\n- Código y modelos → +200 MB\n- **Total: ~1.8 GB** 🔴 → Considerar `python:3.12-slim` + solo dependencias necesarias → **~550 MB**\n\n## Errores comunes\n\n1. **Usar la imagen base completa cuando existe `-slim` o `-alpine`**: `node:20` ocupa 1.1 GB vs. `node:20-alpine` con 130 MB. Siempre evaluá si la versión slim/alpine cubre tu caso.\n\n2. **Instalar dependencias de build en la imagen final**: Herramientas como `gcc`, `make`, `git` o `build-essential` son necesarias para compilar, pero no para correr la app. Usá multi-stage builds para descartarlas.\n\n3. **Cada `RUN` que instala Y borra paquetes en comandos separados**: Si hacés `RUN apt-get install -y curl` y luego en otro `RUN` hacés `rm -rf /var/lib/apt/lists/*`, la primera capa YA guardó el caché de apt. Todo debe ir en un solo `RUN` encadenado con `&&`.\n\n4. **Copiar todo el contexto con `COPY . .` sin `.dockerignore`**: Incluir carpetas como `node_modules/`, `.git/`, `dist/` o archivos de log puede agregar cientos de MB innecesarios. Siempre definí un archivo `.dockerignore`.\n\n5. **No usar `--no-cache` o `--no-install-recommends` en apt**: `apt-get install -y paquete` sin `--no-install-recommends` instala dependencias recomendadas que pueden sumar 50-200 MB extras. Usá `apt-get install -y --no-install-recommends paquete && rm -rf /var/lib/apt/lists/*`.\n\n6. **Ignorar el orden de las capas para aprovechar la caché**: Docker cachea por capa. Si ponés `COPY . .` antes de `RUN npm install`, cualquier cambio en el código invalida la caché de npm. Lo correcto es: `COPY package*.json ./` → `RUN npm install` → `COPY . .`.\n\n## Calculadoras relacionadas\n\n- [Calculadora de tamaño de imagen en píxeles y megapíxeles](/calculadora-tamano-imagen-pixeles-megapixeles) — Útil para estimar el peso de assets gráficos que incluís en tu imagen Docker.\n- [Calculadora de optimización JPG: calidad y tamaño para web](/calculadora-jpg-calidad-tamano-web-optimizacion) — Reducí el tamaño de imágenes estáticas antes de copiarlas al contenedor.\n- [Calculadora de peso de filamento PLA/ABS/PETG por volumen](/calculadora-peso-pla-abs-petg-volumen) — Otro ejemplo de cálculo de peso/volumen en manufactura digital.\n- [Calculadora de tamaño de monitor y distancia ideal](/calculadora-monitor-tamano-distancia-ideal) — Para configurar tu entorno de desarrollo donde corrés Docker.",
  "faq": [
    {
      "q": "¿Cómo calcula Docker el tamaño total de una imagen?",
      "a": "El tamaño total de una imagen Docker es la <strong>suma de todas sus capas descomprimidas</strong>. Cada instrucción `RUN`, `COPY` y `ADD` en el Dockerfile crea una capa inmutable identificada por un hash SHA-256. Cuando corrés `docker images`, el tamaño que ves es la suma en disco de todas esas capas extraídas. Lo que se transfiere al hacer `docker pull` es el tamaño comprimido con gzip, que representa típicamente entre el 40% y el 60% del tamaño en disco. Por ejemplo, una imagen que pesa 500 MB en disco generalmente ocupa unos 200-225 MB en el registro. Esta calculadora trabaja con el tamaño en disco (descomprimido), que es el más relevante para planificar almacenamiento en el host y en registros privados."
    },
    {
      "q": "¿Qué imagen base conviene usar y cuánto pesa cada una?",
      "a": "Los pesos aproximados de las imágenes base más comunes son: <strong>scratch</strong> (0 MB, solo para binarios totalmente estáticos), <strong>Alpine Linux</strong> (~5-7 MB, usa musl libc y BusyBox), <strong>Debian Slim</strong> (~80 MB), <strong>Ubuntu 22.04</strong> (~77 MB), <strong>node:20-alpine</strong> (~170 MB), <strong>node:20-slim</strong> (~240 MB), <strong>node:20</strong> (~920 MB), <strong>python:3.12-slim</strong> (~130 MB), <strong>python:3.12</strong> (~920 MB), <strong>golang:1.22</strong> (~800 MB). Alpine es la opción más liviana, pero su uso de musl libc en vez de glibc puede generar problemas de compatibilidad con librerías Python con extensiones C o binarios compilados para glibc. Siempre verificá compatibilidad antes de migrar a Alpine en producción."
    },
    {
      "q": "¿Qué es un multi-stage build y cuánto puede reducir el tamaño de mi imagen?",
      "a": "Un multi-stage build usa múltiples instrucciones `FROM` en un solo Dockerfile: las primeras etapas compilan o instalan dependencias de desarrollo, y la etapa final solo copia los artefactos necesarios para correr la app. El resultado es que la imagen de producción no incluye compiladores, headers, ni herramientas de build. Los ahorros reales son significativos: una app <strong>Go</strong> pasa de ~800 MB (con toolchain) a ~10-15 MB (binario estático en `scratch`). Una app <strong>Node.js</strong> pasa de ~1,2 GB a ~180-250 MB. Una app <strong>Java con Spring Boot</strong> pasa de ~700 MB a ~250 MB usando JRE slim. En términos generales, un multi-stage bien implementado reduce el tamaño entre un <strong>70% y el 95%</strong> según el lenguaje y las dependencias."
    },
    {
      "q": "¿Cómo veo el peso exacto de cada capa de mi imagen para saber dónde está el problema?",
      "a": "Tenés tres opciones según el nivel de detalle que necesites. La primera es `docker history nombre-imagen`, que muestra el tamaño de cada capa y la instrucción que la generó: útil para identificar rápidamente qué `RUN` o `COPY` pesa más. La segunda es `docker inspect nombre-imagen`, que devuelve un JSON con todos los metadatos incluyendo los hashes de cada capa. La tercera y más poderosa es <strong>Dive</strong>, una herramienta open-source que permite navegar capa por capa de forma visual, ver exactamente qué archivos agrega o modifica cada instrucción, y te da un porcentaje de 'eficiencia' de la imagen. Dive es especialmente útil para detectar archivos de caché de `apt` o `pip` que quedaron en capas anteriores y que podrían eliminarse con `--no-install-recommends` o `rm -rf /var/lib/apt/lists/*`."
    },
    {
      "q": "¿Por qué mi imagen pesa más de lo que esperaba aunque usé Alpine?",
      "a": "Hay varios errores comunes que inflan el tamaño incluso partiendo de Alpine. El más frecuente es <strong>no limpiar el caché de apk</strong> después de instalar paquetes: el comando correcto es `apk add --no-cache paquete` o agregar `rm -rf /var/cache/apk/*` al final del mismo `RUN`. El segundo error es usar múltiples instrucciones `RUN` en vez de encadenarlas con `&&`: cada `RUN` crea una capa nueva, y los archivos eliminados en una capa posterior no reducen el tamaño de la imagen porque la capa original ya fue sellada. El tercer error es copiar directorios completos con `COPY . .` en vez de ser selectivo, lo que incluye `node_modules`, `.git`, archivos de test y configuración local que no deberían estar en producción. Usá siempre un `.dockerignore` bien configurado."
    },
    {
      "q": "¿Las capas se comparten entre imágenes en el mismo host Docker?",
      "a": "Sí, y es una de las ventajas más importantes del sistema de capas de Docker. El driver de almacenamiento por defecto en Linux moderno es <strong>overlay2</strong>, que almacena cada capa una sola vez identificada por su hash SHA-256. Si tenés 10 microservicios basados en `node:20-alpine`, la capa de Alpine (~7 MB) y la de Node (~163 MB) se almacenan una única vez en el host, no 10 veces. En un clúster Kubernetes con 20 nodos, esto significa que el segundo pull de la misma imagen base es prácticamente instantáneo porque las capas ya están en disco. Esta lógica de compartir capas también aplica entre versiones de la misma imagen: si cambiás solo el código de tu app (la última capa), Docker reutiliza todas las capas anteriores del cache."
    },
    {
      "q": "¿Existe un límite de capas en una imagen Docker?",
      "a": "Con el driver <strong>overlay2</strong> (el default en Linux moderno con kernel 4.x+), el límite es de <strong>128 capas por imagen</strong>. En práctica casi nunca se llega a ese número, pero es un incentivo más para consolidar comandos. Cada instrucción `RUN`, `COPY` y `ADD` crea una capa nueva: un Dockerfile con 50 instrucciones `RUN` separadas tiene 50 capas. La buena práctica es encadenar comandos relacionados con `&&` y `\\ ` para multilínea. Por ejemplo: en vez de tres `RUN apt-get install`, un solo `RUN apt-get update && apt-get install -y paquete1 paquete2 && rm -rf /var/lib/apt/lists/*`. Esto reduce capas, elimina el caché de apt en la misma capa donde se instaló y hace la imagen más eficiente."
    },
    {
      "q": "¿Cómo afecta el tamaño de la imagen a los tiempos de deployment en Kubernetes o en un pipeline de CI/CD?",
      "a": "El impacto es directo y se siente en producción. En un nodo de Kubernetes con ancho de banda típico de <strong>1 Gbps</strong> a un registro como ECR o GCR, descargar 1 GB toma aproximadamente 8-10 segundos en condiciones ideales, pero con la latencia real de los registros y el overhead de descompresión, puede ser 30-90 segundos. Una imagen de 100 MB se descarga en 3-8 segundos. En escenarios de <strong>autoscaling</strong> ante picos de tráfico, la diferencia entre un pod que arranca en 10 segundos y uno que tarda 90 segundos puede ser crítica. En pipelines de GitHub Actions, CircleCI o GitLab CI, el pull de la imagen base es un paso que se repite en cada job: bajar de 900 MB a 150 MB puede ahorrar 2-3 minutos por build, lo que en 100 builds diarios son más de 200 minutos de compute time."
    },
    {
      "q": "¿Cuánto cuesta almacenar imágenes en Docker Hub, ECR o GitHub Container Registry?",
      "a": "Los costos varían por proveedor. <strong>Docker Hub</strong>: el plan gratuito permite 1 repositorio privado con límites de pull; el plan Pro (USD 5/mes) incluye repositorios privados ilimitados sin límite de almacenamiento explícito, pero impone rate limits en pulls. <strong>Amazon ECR</strong>: USD 0,10 por GB/mes para imágenes privadas; los primeros 500 MB/mes son gratis. La transferencia de datos desde ECR a servicios AWS en la misma región es gratuita. <strong>GitHub Container Registry (ghcr.io)</strong>: gratis para repositorios públicos; para privados se consume del storage de GitHub Actions (el plan gratuito incluye 500 MB de storage y 1 GB de transferencia/mes). <strong>Google Artifact Registry</strong>: USD 0,10/GB/mes con los primeros 0,5 GB gratis. En todos los casos, tener imágenes más pequeñas reduce directamente la factura de storage y los costos de transferencia de datos."
    },
    {
      "q": "¿Qué es una imagen distroless y cuándo conviene usarla?",
      "a": "<strong>Distroless</strong> son imágenes mantenidas por Google (disponibles en gcr.io/distroless) que contienen solo el runtime necesario para correr una aplicación, sin shell, sin gestor de paquetes, sin utilidades del sistema operativo. El resultado es una imagen más pequeña y con una <strong>superficie de ataque mínima</strong> desde el punto de vista de seguridad: sin bash, no hay forma de ejecutar comandos arbitrarios si un atacante compromete el contenedor. Pesos típicos: `distroless/java17` (~220 MB), `distroless/python3` (~50 MB), `distroless/nodejs20` (~165 MB), `distroless/static` (~2 MB para binarios estáticos). La contrapartida es que el debugging se complica mucho porque no tenés shell para entrar al contenedor. La recomendación: usalas en producción con una imagen de debug como segunda etapa solo para troubleshooting (`image: gcr.io/distroless/python3:debug`)."
    },
    {
      "q": "¿Cómo limpio el espacio ocupado por imágenes Docker en mi máquina o servidor?",
      "a": "Docker acumula imágenes, capas intermedias y caché de build que pueden ocupar decenas de GB. Los comandos clave para limpiar son: <strong>`docker image prune`</strong> elimina solo imágenes 'dangling' (sin tag, generalmente capas intermedias de builds anteriores). <strong>`docker image prune -a`</strong> elimina todas las imágenes que no tienen ningún contenedor corriendo o detenido que las use. <strong>`docker system prune -a`</strong> es el más agresivo: elimina imágenes sin usar, contenedores detenidos, redes no usadas y todo el caché de build. <strong>`docker builder prune`</strong> limpia específicamente el caché del builder de BuildKit. Docker guarda todo en `/var/lib/docker` por defecto en Linux. Podés ver cuánto espacio usa cada componente con `docker system df`. En servidores de CI/CD es recomendable ejecutar `docker system prune -f` al final de cada pipeline para evitar que el disco se llene."
    },
    {
      "q": "¿Qué es BuildKit y cómo ayuda a optimizar el tamaño y el tiempo de build?",
      "a": "<strong>BuildKit</strong> es el backend de build moderno de Docker, habilitado por defecto desde Docker 23.x. Sus principales ventajas para optimización son: <strong>caché de capas más inteligente</strong> que el builder clásico, detectando qué cambió realmente en vez de invalidar todo desde el punto del cambio; <strong>builds en paralelo</strong> de múltiples etapas en multi-stage builds; <strong>caché de mounts</strong> con `--mount=type=cache` que permite cachear directorios como `/root/.cache/pip` o `node_modules` entre builds sin incluirlos en la imagen final (esto solo reduce tiempo de build, no el tamaño de la imagen); y <strong>secret mounts</strong> con `--mount=type=secret` para usar credenciales durante el build sin que queden grabadas en ninguna capa. Para habilitar BuildKit en versiones anteriores de Docker: `export DOCKER_BUILDKIT=1` antes de `docker build`."
    }
  ],
  "sources": [
    {
      "name": "Docker Documentation – Best practices for writing Dockerfiles",
      "url": "https://docs.docker.com/develop/develop-images/dockerfile_best-practices/"
    }
  ],
  "relatedSlugs": [
    "calculadora-peso-pla-abs-petg-volumen"
  ],
  "dataUpdate": {
    "frequency": "never",
    "lastUpdated": "2026-04-18",
    "updateType": "manual",
    "notes": "Fórmula fija, no requiere actualización de data externa",
    "source": "Docker Documentation – Best practices for writing Dockerfiles",
    "sourceUrl": "https://docs.docker.com/develop/develop-images/dockerfile_best-practices/"
  },
  "seoKeywords": [
    "imagen docker (capas/tamaño)",
    "calculadora imagen docker 2026",
    "imagen docker capas peso mb"
  ]
}
