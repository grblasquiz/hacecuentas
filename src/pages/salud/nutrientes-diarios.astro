---
/**
 * Hub de decisión — "¿Cuánto necesito por día de cada nutriente?"
 * Reemplaza 9 URLs de calculadora suelta (ver hub.replaces).
 * Los datos viven en src/lib/hubs/nutrientes-diarios.ts.
 */
export const prerender = true;

import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import NutrientesDiariosExperience from '../../components/generated/NutrientesDiariosExperience.astro';
import { hub, SODIO_LIMITES, AZUCAR, CAFEINA, MAGNESIO_SUPL } from '../../lib/hubs/nutrientes-diarios';

const CANONICAL = `https://hacecuentas.com/${hub.slug}`;

const schema = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'FAQPage',
      mainEntity: hub.faq.map((f) => ({
        '@type': 'Question',
        name: f.q,
        acceptedAnswer: { '@type': 'Answer', text: f.a },
      })),
    },
    {
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Inicio', item: 'https://hacecuentas.com/' },
        { '@type': 'ListItem', position: 2, name: hub.silo, item: `https://hacecuentas.com${hub.siloHref}` },
        { '@type': 'ListItem', position: 3, name: hub.h1, item: CANONICAL },
      ],
    },
  ],
};
---

<Layout
  title={hub.title}
  description={hub.description}
  canonical={CANONICAL}
  schema={schema}
  audience={hub.audience}
  lang="es"
  ogType="article"
  articleSection={hub.silo}
>
  <Header />
  <NutrientesDiariosExperience data={hub} />
  <Footer />
</Layout>

