---
export const prerender = true;
import Layout from '../../../layouts/Layout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import DecisionHub from '../../../components/hub/DecisionHub.astro';
import { hub } from '../../../lib/hubs/uy/estudio-y-vida-cotidiana';
const canonical = `https://hacecuentas.com/${hub.slug}`;
const schema = {'@context':'https://schema.org','@type':'FAQPage',mainEntity:hub.faq.map(f=>({'@type':'Question',name:f.q,acceptedAnswer:{'@type':'Answer',text:f.a}}))};
---
<Layout title={hub.title} description={hub.description} canonical={canonical} schema={schema} audience="UY" lang="es" ogType="article" articleSection={hub.silo}>
  <Header /><DecisionHub data={hub} /><Footer />
</Layout>
<script>
  import { formulaMap } from '../../../lib/hub-formulas-generated/uy-estudio-y-vida-cotidiana';
  import { adaptGeneratedHubResult } from '../../../lib/generated-hub-adapter';
  const configs = {"c1":{"formulaId":"edad-perro-humano-raza-tamano","fields":["v1","v2"],"outputs":[{"id":"resultado","label":"Equivalente humano","primary":true},{"id":"resumen","label":"Interpretación"}],"title":"Edad de perro en años humanos según tamaño — Uruguay"},"c2":{"formulaId":"calculadora-promedio-escolaridad-udelar-uruguay","fields":["notas","creditos","modo","objetivoPromedio","creditosRestantes"],"outputs":[{"id":"promedio","label":"Promedio de escolaridad","format":"text","primary":true},{"id":"notaNecesaria","label":"Nota promedio necesaria en lo restante","format":"text","hideWhenEmpty":true},{"id":"categoria","label":"Categoría conceptual","format":"text"},{"id":"totalCreditos","label":"Créditos aprobados","format":"number"},{"id":"detalle","label":"Detalle","format":"text"}],"title":"Calculadora de promedio de escolaridad — UdelaR (Uruguay)"}};
  function register() {
    const H = (window as any).HC_HUB;
    if (!H) return;
    H.onCompute((all:any, selected:any) => adaptGeneratedHubResult(all, selected, configs as any, formulaMap, { locale: 'es', format: 'unit' }));
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', register, { once: true });
  else register();
</script>
