---
export const prerender = true;
import FindeHubPage from '../../components/FindeHubPage.astro';
import { FINDE_HUBS } from '../../lib/fin-de-semana-hubs';
const hub = FINDE_HUBS.find((h) => h.slug === 'fiestas-y-reuniones')!;
---
<FindeHubPage hub={hub} />
