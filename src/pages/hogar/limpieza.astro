---
export const prerender = true;
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import LimpiezaHogarExperience from '../../components/generated/LimpiezaHogarExperience.astro';
import { hub } from '../../lib/hubs/limpieza';
const canonical='https://hacecuentas.com/hogar/limpieza';
const schema={'@context':'https://schema.org','@type':'WebApplication',name:hub.title,url:canonical,operatingSystem:'Web'};
---
<Layout title={hub.title} description={hub.description} canonical={canonical} schema={schema} lang="es">
  <Header />
  <LimpiezaHogarExperience />
  <Footer />
</Layout>
