---
/**
 * Hub de decisión DO — "Qué le debe tu negocio a la DGII".
 */
export const prerender = true;

import Layout from '../../../layouts/Layout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import NegocioDgiiDominicanaExperience from '../../../components/generated/NegocioDgiiDominicanaExperience.astro';
import RegimeJourney from '../../../components/RegimeJourney.astro';
import {
  hub,
  ITBIS,
  TASA_ISR_SOCIEDADES,
  ANTICIPOS,
  RETENCION_HONORARIOS,
  ITF,
  PROPINA_LEGAL,
  MORA,
} from '../../../lib/hubs/do/negocio-dgii';

const CANONICAL = `https://hacecuentas.com/${hub.slug}`;

const schema = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'FAQPage',
      mainEntity: hub.faq.map((f) => ({
        '@type': 'Question',
        name: f.q,
        acceptedAnswer: { '@type': 'Answer', text: f.a },
      })),
    },
    {
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Inicio', item: 'https://hacecuentas.com/' },
        { '@type': 'ListItem', position: 2, name: hub.silo, item: `https://hacecuentas.com${hub.siloHref}` },
        { '@type': 'ListItem', position: 3, name: hub.h1, item: CANONICAL },
      ],
    },
  ],
};
---

<Layout
  title={hub.title}
  description={hub.description}
  canonical={CANONICAL}
  schema={schema}
  audience="DO"
  lang="es"
  ogType="article"
  articleSection={hub.silo}
>
  <Header />
  <NegocioDgiiDominicanaExperience data={hub} />
  <RegimeJourney country="República Dominicana" regime="RST" headline="RST por ingresos o compras: empezá por la modalidad correcta." intro="El Régimen Simplificado de Tributación cambia según seas persona física, jurídica o comercio. La ruta ordena elegibilidad, ISR e ITBIS sin mezclarlos." steps={['Elegí modalidad del RST','Proyectá ingresos o compras','Compará ISR e ITBIS']} links={[{href:'/do/impuestos/negocio-dgii',label:'Simular negocio DGII',note:'RST y régimen ordinario'},{href:'/do/calculadora-isr-republica-dominicana',label:'Calcular ISR',note:'Escala y exención contributiva'},{href:'/do/impuestos/index',label:'Ordenar vencimientos',note:'Herramientas fiscales dominicanas'}]} tone="amber" />
  <Footer />
</Layout>
