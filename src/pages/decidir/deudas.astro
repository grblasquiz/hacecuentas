---
export const prerender = true;

import DecisionHubPage from '../../components/DecisionHubPage.astro';
import { DECISION_HUBS } from '../../lib/decisions/hubs';

const hub = DECISION_HUBS.find((h) => h.slug === 'deudas')!;
---

<DecisionHubPage hub={hub} />
