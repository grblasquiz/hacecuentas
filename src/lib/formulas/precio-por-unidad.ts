export interface Inputs { precioPaquete:number; unidades:number; contenidoPorUnidad?:number; }
export function precioPorUnidad(i:Inputs){const p=Number(i.precioPaquete),u=Number(i.unidades),c=Number(i.contenidoPorUnidad||1);if(!(p>0&&u>0&&c>0))throw new Error('Precio, unidades y contenido deben ser mayores que cero.');const porUnidad=p/u,porContenido=p/(u*c);return{precioPorUnidad:Number(porUnidad.toFixed(2)),precioPorContenido:Number(porContenido.toFixed(4)),contenidoTotal:Number((u*c).toFixed(2)),detalle:`El paquete trae ${u} unidades (${(u*c).toLocaleString('es-AR')} de contenido total). Cada unidad cuesta $${porUnidad.toFixed(2)}.`,_insight:{title:'Llevá todo a una base comparable',text:'Para comparar packs distintos, usá precio por unidad de contenido y no sólo el precio del envase.',tone:'neutral',icon:'🏷️'}}}
