export interface Inputs { fecha_inicio:string; dias_plazo:number; incluir_sabado:'no'|'si'; }
export interface Outputs { fecha_limite:string; dias_habiles_contados:number; advertencia:string; resumen:string; _insight?:any; }
export function compute(i:Inputs):Outputs { const p=String(i.fecha_inicio||'').split('-').map(Number); if(p.length!==3) throw new Error('Ingresá una fecha válida.'); const d=new Date(p[0],p[1]-1,p[2]); if(isNaN(d.getTime())) throw new Error('Ingresá una fecha válida.'); const objetivo=Math.max(1,Math.floor(Number(i.dias_plazo)||1)); let n=0; d.setDate(d.getDate()+1); while(n<objetivo){const wd=d.getDay(); if(wd!==0&&(i.incluir_sabado==='si'||wd!==6))n++; if(n<objetivo)d.setDate(d.getDate()+1);} const fecha=d.toLocaleDateString('es-CO',{day:'2-digit',month:'2-digit',year:'numeric'}); const advertencia='No descuenta festivos nacionales, cierres propios de la entidad ni cambios del cronograma publicados en SECOP.'; return {fecha_limite:fecha,dias_habiles_contados:n,advertencia,resumen:`El vencimiento orientativo es ${fecha}.`,_insight:{title:'Vencimiento orientativo SECOP',text:`Contando **${n} días hábiles** desde el día siguiente a la fecha de inicio, el vencimiento cae el **${fecha}**. ${advertencia}`,tone:'warn',icon:'📑'}}; }
