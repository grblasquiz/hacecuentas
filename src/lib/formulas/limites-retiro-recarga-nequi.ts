export interface Inputs { operacion:'recarga_nequi'|'recarga_bancolombia'|'retiro_cajero'|'retiro_corresponsal'; monto:number; acumulado_mes:number; tipo_nequi:'deposito'|'cuenta'; }
export interface Outputs { permitido:string; limite_operacion:number; margen_disponible:number; costo_nequi:number; recomendacion:string; _insight?:any; }
export function compute(i:Inputs):Outputs { const limites:any={recarga_nequi:1000000,recarga_bancolombia:3000000,retiro_cajero:2700000,retiro_corresponsal:1000000}; const limite=limites[i.operacion]||0, monto=Math.max(0,Number(i.monto)||0), acumulado=Math.max(0,Number(i.acumulado_mes)||0), topeMes=i.tipo_nequi==='deposito'?11024727:Infinity, margen=Math.max(0,Math.min(limite,topeMes-acumulado)); const permitido=monto>=10000&&monto<=margen?'Sí, dentro de los límites orientativos':'No o requiere ajustar el monto'; const recomendacion=monto<10000?'El mínimo usual en puntos físicos y cajeros es $10.000.':monto>margen?'Reducí el monto o verificá tus topes en la app Nequi.':'Verificá disponibilidad de efectivo y los límites configurados en tu app.'; return {permitido,limite_operacion:limite,margen_disponible:Math.round(margen),costo_nequi:0,recomendacion,_insight:{title:'Límite orientativo Nequi',text:`Para esta operación el máximo de referencia es **$${limite.toLocaleString('es-CO')}**. ${recomendacion}`,tone:permitido==='Sí, dentro de los límites orientativos'?'good':'warn',icon:'💜'}}; }
