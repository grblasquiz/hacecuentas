export interface Inputs { casosFavorables:number; casosPosibles:number; intentos?:number; }
export function probabilidadEvento(i:Inputs){const f=Number(i.casosFavorables),t=Number(i.casosPosibles),n=Number(i.intentos||1);if(!(t>0&&n>=1)||f<0||f>t)throw new Error('Los casos favorables deben estar entre 0 y los casos posibles.');const p=f/t,alguna=1-Math.pow(1-p,n),ninguna=Math.pow(1-p,n);return{probabilidad:Number((p*100).toFixed(2)),probabilidadAlguna:Number((alguna*100).toFixed(2)),probabilidadNinguna:Number((ninguna*100).toFixed(2)),detalle:`En un intento: ${(p*100).toFixed(2)}%. En ${n} intento${n===1?'':'s'} independientes, la probabilidad de que ocurra al menos una vez es ${(alguna*100).toFixed(2)}%.`,_insight:{title:'La independencia es un supuesto',text:'La fórmula de varios intentos sólo vale si cada intento mantiene la misma probabilidad y no depende de los anteriores.',tone:'neutral',icon:'🎲'}}}
