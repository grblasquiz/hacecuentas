# Cambios de infraestructura (no de contenido de URL):
# - 11 URLs zombie (JSON-vivo/URL-301) removidas de sitemaps (filtro _redirects en generate-sitemap.ts)
# - content-policy.ts: nuevo helper isSensitiveCalculator() (afecta render de meta-robots via API compuesta, misma logica)
# - indexnow-push.py: modo --changed
# Ninguna calc/guia cambio de contenido en esta ejecucion.
